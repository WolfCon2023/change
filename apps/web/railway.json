{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build --workspace=@change/shared && npm run build --workspace=@change/web"
  },
  "deploy": {
    "startCommand": "npx serve -s apps/web/dist -l $PORT"
  }
}
